services:
  backend:
    build: ./backend
    container_name: claude-code-mobile-backend
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=production
